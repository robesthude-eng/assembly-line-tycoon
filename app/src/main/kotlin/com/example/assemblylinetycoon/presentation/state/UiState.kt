package com.example.assemblylinetycoon.presentation.state

/** Маркер UI-состояния: неизменяемый снимок экрана. */
interface UiState

/** Маркер намерения пользователя (MVI Intent). */
interface UiIntent

/** Маркер разового эффекта: навигация, тост, показ рекламы. */
interface UiEffect
