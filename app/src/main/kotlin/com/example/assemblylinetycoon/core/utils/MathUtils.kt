package com.example.assemblylinetycoon.core.utils

/**
 * Формулы прогрессии (стоимость апгрейда, длительность крафта).
 *
 * Заполняется на этапе «Domain Models & Logic». Сейчас — только точка расширения,
 * чтобы формулы не расползлись по слоям.
 */
object MathUtils {
    // TODO(этап 2): upgradeCost(base: Long, level: Int): Long
    // TODO(этап 2): craftDuration(base: Long, level: Int): Long
}
