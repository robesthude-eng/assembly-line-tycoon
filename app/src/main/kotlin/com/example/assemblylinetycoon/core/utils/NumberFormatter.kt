package com.example.assemblylinetycoon.core.utils

/**
 * Форматирование больших чисел для интерфейса (1.2K, 3.4M, 5.6B…).
 *
 * Реализация появится вместе с первым экраном. Вынесено в core, потому что
 * форматирование нужно и презентации, и логам, но не является игровой логикой.
 */
object NumberFormatter {
    // TODO: format(value: Long): String
}
